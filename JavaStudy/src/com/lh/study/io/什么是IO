BIO BlockIO，传统IO，阻塞IO

NIO是New IO的简称，与旧式基于流的IO方法相比，他是一套新的Java IO标准。
Java 1.4中被纳入到JDK中的，具有以下特性：
1、NIO是基于块(Block)的，它以块为基本单位处理数据
2、为所有的原始类型提供(Buffer)缓存支持
3、增加通道（Channel）对象，作为新的原始IO抽象
4、支持锁和内存映射文件的文件访问接口
5、提供了基于Selector的异步网络IO

相对传统IO的特点：不会等待客户端准备数据，会开一个或多个的线程来监听客户端数据是否已经准备好
如果准备好了，再通知开始处理。不会因客户端网络问题而产生服务端线程阻塞影响服务器性能。


AIO 异步IO
1、读完了再通知我
2、不会加快IO，只是在读完后进行处理
3、使用回调函数，进行业务处理